import { preventDefault } from 'dummy/helpers/prevent-default';
import { module, test } from 'qunit';

module('Unit | Helper | prevent default');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = preventDefault([42]);
  assert.ok(result);
});
