import { stopPropagation } from 'dummy/helpers/stop-propagation';
import { module, test } from 'qunit';

module('Unit | Helper | stop propagation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stopPropagation([42]);
  assert.ok(result);
});
