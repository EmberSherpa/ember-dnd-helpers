import { dragSet } from 'dummy/helpers/drag-set';
import { module, test } from 'qunit';

module('Unit | Helper | drag set');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dragSet([42]);
  assert.ok(result);
});
