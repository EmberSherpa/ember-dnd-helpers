import { dropEffect } from 'dummy/helpers/drop-effect';
import { module, test } from 'qunit';

module('Unit | Helper | drop effect');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dropEffect([42]);
  assert.ok(result);
});
