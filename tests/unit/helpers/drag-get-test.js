import { dragGet } from 'dummy/helpers/drag-get';
import { module, test } from 'qunit';

module('Unit | Helper | drag get');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dragGet([42]);
  assert.ok(result);
});
